# 测试与验证

用途：记录当前项目的验证策略、可执行命令和测试缺口，供 foundation、increment、checkpoint 使用。

## 当前状态

- Project：AI Native Product Builder Portfolio
- Latest visual foundation：`VU-001` 已建立 night / mist / porcelain / paper / glass / border / shadow / radius 共享语义层，并将详情页轻 dither 提取为全站可复用纹理；本轮没有改变页面结构、首页吸附和详情页渐变停靠点。
- Latest visual release：`VU-002` 至 `VU-006` 已完成；首页、Projects 和 Ecommerce Review Copilot 详情页统一为深色品牌层、灰蓝雾层、白色阅读层，首页旧滚动吸附已取消；Projects 标题和卡片比例已按用户反馈回调，公开开发占位已清理。
- Status：Foundation complete；first formal Case Study content and detail display skeleton available；homepage capability-driven personal brand IA available with refined Typography System, dark glass Hero, AIPM Capability System, AI Product Operating Process, and AIPM method sample module；homepage AIPM Capability System now uses a desktop one-screen semicircle wheel that keeps the central `AI Product Builder` circle, shows only three cards at a time, and supports wheel, horizontal trackpad, and left/right keyboard switching；global typography now uses Apple-first font stack and text gray tokens use Apple-ish cool grays；Hero duplicate kicker and keyword tags removed；homepage key section headings and subtitles aligned；shared dark header `HAOLIN WU` editorial name now uses visible word spacing, low-contrast solid editorial letters, and segmented underline gaps so the line appears only between letters, in the word space, and on both sides without crossing or bleeding through letter bodies；shared dark header is now fully transparent as an absolute overlay, with a clearer segmented underline and a slightly lighter editorial name；sample 01 detail page now uses dark hero handoff, light reveal, floating white content shell, and a right-side summary card that starts in-column then sticks near the vertical center while reading; sticky failure caused by overflow/grid rail constraints has been fixed；sample 01 detail Hero body has been moved down after the transparent header so its back button, title, metadata cards, and judgment-chain panel are not covered by the `HAOLIN WU` editorial name；sample 01 detail Hero title now uses a deliberate two-line split with a subtle cool blue-purple accent on `决策 AI Copilot`；sample 01 detail Hero background now has denser 12-stop left/right bottom transitions while keeping the central white diffusion parameters unchanged；sample 01 detail judgment-chain panel now uses the Apple-first sans font stack for its English kicker and number labels；sample 01 detail content-shell side background handoff now uses a longer lower-contrast mist band plus subtle SVG dither to reduce visible gradient banding, and the outer page gutters now use dark low-opacity non-uniform depth grids with a fully transparent top clear area while the content shell remains clean.
- Latest visual increment：Projects keeps the compact dark Hero and smooth night-to-mist-to-paper handoff, while its title now stays within 40–64px and its single case uses the original compact vertical-card structure in a responsive 3/2/1 grid；public development placeholders, raw route metadata, and unavailable evidence cards are no longer rendered.
- 当前可以执行文档级验证、Astro 内容 schema 校验、TypeScript / Astro check 和静态构建验证。
- 当前目录可正常执行 `git status`；本次会话相关修改仍处于未暂存状态，版本状态已通过差异检查与文档检索确认。

## 当前验证方式

本阶段验证以下内容：

```text
1. docs/prd.md 是否已经包含真实 Portfolio PRD。
2. docs/portfolio-content-strategy.md 是否存在并定义能力证明链。
3. docs/case-study-template-schema.md 是否存在并定义 Canonical Case Study Schema。
4. docs/content-model.md 是否定义内容实体、docs/apps 边界和内容使用规则。
5. docs/project-portfolio-index.md 是否定义 Featured 项目和项目池。
6. docs/visual-system.md 是否定义视觉方向、组件表达、动效边界和 Visual QA Checklist。
7. docs/tech-stack-decision.md 是否明确 primary route 为 Astro-powered Editorial Product Experience System。
8. AGENTS.md 与 CLAUDE.md 是否同步。
9. apps/ai-native-product-builder-portfolio/ 是否存在最小 Astro 基座。
10. Astro Content Collections / Zod schema 是否能校验 placeholder 内容。
11. Home / Projects / Case Study Detail 是否能静态构建。
```

## Foundation 已补充

当前可执行命令：

```text
install command: pnpm install
dev command: pnpm run dev
build command: pnpm run build
check command: pnpm run check
lint command: pnpm run lint
test command: not configured in foundation
content validation command: covered by Astro Content Collections / Zod schema via pnpm run check and pnpm run build
manual acceptance checklist: Home / Projects / Case Study shell, responsive layout, content schema errors, no forbidden visual style
```

本地当前环境说明：

```text
node command: /Users/juzihaidou/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node
pnpm command: /Users/juzihaidou/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm
```

如果系统 PATH 没有 Node / pnpm，需要先安装 Node.js，或显式使用 Codex bundled Node / pnpm。

## 视觉验收清单

进入页面、组件或视觉增量时，必须额外检查：

- 是否立即传达 AI Native Product Builder 定位。
- 是否让 Product Judgment 和 AI Workflow 更容易理解。
- 是否遵守 `docs/visual-system.md` 的色彩、Typography、Layout、Component、Motion 和 Anti-patterns。
- 是否避免技能 Badge 墙、项目截图墙、AI 科幻视觉、复杂 3D、粒子背景和动画 showcase。
- 是否尊重 `prefers-reduced-motion`。
- 是否保持响应式可读性和键盘可访问性。

## 内容验收清单

每个 Case Study 至少检查：

- 是否有明确用户和问题。
- 是否解释 Why AI。
- 是否定义 AI Boundary。
- 是否包含 Human-in-the-loop。
- 是否有 AI Workflow / Agent Design。
- 是否解释技术取舍。
- 是否包含 Product Metrics 和 AI Metrics。
- 是否保留 Outcome 和 Reflection。
- 是否避免技能 Badge 墙。
- 是否没有编造 demo、截图、指标或真实用户反馈。

## 首页验收清单

首页必须让访问者在 5-10 秒内理解：

```text
Who: 巫浩林 / AI Product Manager / AI Product Builder
What: 将业务问题转化为可验证的 AI 产品
How: Problem Framing -> AI Judgment -> Workflow Design -> Prototype -> Evaluation -> Iteration
Homepage IA: Hero -> AIPM Capability System -> How I Build AI Products -> 方法样本 / 能力样本
```

## 命令清单

| 命令 | 用途 | 何时运行 | 当前状态 |
| --- | --- | --- | --- |
| `pnpm install` | 安装依赖 | clone 或清理 node_modules 后 | 已创建 |
| `pnpm run dev` | 本地开发预览 | foundation 后 | 已创建 |
| `pnpm run build` | 生产构建 | foundation 后每次重要改动 | 已创建并通过 |
| `pnpm run check` | Astro / TypeScript / 内容 schema 校验 | foundation 后每次重要改动 | 已创建并通过 |
| `pnpm run lint` | 基础检查；当前等同 `astro check` | foundation 后每次重要改动 | 已创建并通过 |
| In-app browser screenshots / responsive QA | 响应式、整页连续性和交互验收 | 页面或视觉组件阶段 | 已执行并通过 |

## 最近验证记录

| 日期 | 变更 | 执行命令或方式 | 结果 |
| --- | --- | --- | --- |
| 2026-07-10 | VU-006 案例索引比例回调与公开占位清理 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；在 1710×960、1440×900、1024×768、390×844 检查 Projects H1、标题行位置、网格列数、卡片尺寸、CTA 和水平溢出；可见检查桌面 / 移动端顶部深色到浅色案例区过渡和紧凑卡片；实际点击 CTA；检查首页空面板 / `Live Map`、详情页 slug / 待就绪资源卡和浏览器 error/warn 日志；源码复核 hover、focus 与 reduced-motion 规则 | 通过；21 个 Astro 文件 0 errors / 0 warnings / 0 hints并生成 3 条静态路由；H1 为 64/60/49.6/40px，首尾同一行且无孤字；网格为 3/3/2/1 列，1440 卡宽约 386.7px、圆角 12px；所有视口无水平溢出；CTA 进入 `/projects/ecommerce-review-copilot/`；公开页不再渲染已确认的占位文案和未就绪资源卡，真实 Outcome、限制和待验证指标保留；浏览器错误日志为空 |
| 2026-07-10 | VU-005 全站视觉 QA 与微调 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；在 1440×900、1280×800、1024×768、390×844 检查首页、Projects、详情页；分段生成三页长图并缩小到约 18%；真实操作导航锚点、轮盘键盘/滚轮、首页与 Projects CTA、详情返回；测量 Sticky、文字对比度、水平溢出并读取 error/warn 日志；源码核对 `prefers-reduced-motion` 降级规则 | 通过；21 个 Astro 文件 0 errors / 0 warnings / 0 hints，构建 `/`、`/projects/`、`/projects/ecommerce-review-copilot/`；所有视口 `scrollWidth === clientWidth`；三页缩略图显示统一深→雾→白叙事；轮盘、CTA、返回和锚点正常；Sticky 在 5 个滚动样本 top 均为 181px；关键文字对比度 4.92:1–18.18:1；减少动效时正文始终可见，非必要动画、过渡和位移关闭后不影响理解；错误日志为空 |
| 2026-07-10 | VU-004 案例详情页视觉对齐 | `check / build / lint`；浏览器检查桌面与 390px 的 Hero 层数、共享圆角/阴影/白壳、外侧网格、Sticky 与单列降级；1024px 可见检查标题断行和双栏比例 | 通过；详情页仍有 6 层 Hero 背景、2 个外侧网格、94% 白壳；Hero 面板/内容壳/正文卡圆角为 28/28/12px；1440 Sticky 稳定，390 为 static 单列；1024 标题两行各自单行显示且无溢出 |
| 2026-07-10 | VU-003 Projects 单案例索引统一 | `check / build / lint`；浏览器检查 1440、1280、1024、390 的暗色页眉、Hero 明暗交接、单案例卡列宽、角色栏和 CTA；整页缩略图检查 | 通过；仅 1 张正式案例卡；桌面为白色主内容 + 深色证明栏，390 单列；CTA 为 `/projects/ecommerce-review-copilot/` 并完成真实导航；无横向溢出；移动标题换行已修正 |
| 2026-07-10 | VU-002 首页由深到浅的完整视觉弧线 | `check / build / lint`；1440×900 与 390×844 浏览器检查 section 高度、背景层、方法样本双面卡、联系区、滚动吸附和水平溢出；整页缩略图检查；轮盘键盘与滚轮回归 | 通过；根节点 `scrollSnapType:none`；Hero 保持 100svh，后续自然高度；整页缩略图为连续深→深蓝→灰蓝→白；390 无溢出且页眉不遮挡姓名；轮盘可从 01/02/03 切到 02/03/04 |
| 2026-07-10 | VU-001 建立全站视觉语义基础 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；启动 `http://127.0.0.1:4350/` 本地预览；浏览器检查首页、Projects 与 Ecommerce Review Copilot 详情页的 computed token、背景层、卡片圆角、内容壳、外侧网格、Sticky 摘要、水平溢出和错误日志 | 通过；21 个 Astro 文件为 0 errors / 0 warnings / 0 hints；构建生成 `/`、`/projects/`、`/projects/ecommerce-review-copilot/`；1280px 下三页 `scrollWidth` 等于 `clientWidth`；详情页仍有 6 层 Hero 渐变、2 个外侧网格、`rgba(255,255,255,.94)` 内容壳和 sticky 摘要；浏览器错误日志为空 |
| 2026-07-10 | UX-023 首页前三屏滚动吸附 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4350/` 启动；浏览器以 1440x900 验证根节点 computed scroll snap、前三屏的 target 属性、从相邻屏滚动后的落点，以及进入方法样本后的自由滚动；再以 390x844 验证移动回退 | 通过；三项命令均通过，`astro check` 为 0 errors / 0 warnings / 0 hints；1440x900 下根节点为 `scrollSnapType:y`，前三屏均为 `scrollSnapAlign:start` / `scrollSnapStop:always`，相邻滚动后稳定在 `scrollY=900` 和 `1800`；进入方法样本可停在 `scrollY=2900`，没有回拉；390x844 下 scroll snap 为 none、水平溢出为 0 |
| 2026-07-10 | UX-022 首页前三屏满视口与垂直重心校准 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4350/` 启动；浏览器检查 1440x900 与移动 390x844 视口下前三段边界、内容组中心、响应式回退与水平溢出 | 通过；三项命令均通过，`astro check` 为 0 errors / 0 warnings / 0 hints；1440x900 下 Hero、能力系统、构建流程的 `top` 分别为 0/900/1800，三段均为 900px 高，水平溢出为 0；内容组中心分别落在 473/1389/2268，相对 section 中心下移约 23/39/18px；移动 390x844 下三段均为自然高度、构建流程 display 为 block，水平溢出为 0 |
| 2026-07-10 | UX-021 首页 AIPM 能力系统半圆轮盘 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4344/` 启动；浏览器检查 `/#capabilities` 的桌面 1280x720 和移动 390x844 视口；用方向键、滚轮上下和水平滚动验证单步轮播；检索 DOM slot、`aria-hidden`、`pointer-events`、section 高度、核心圆位置和水平溢出 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；桌面 1280x720 下能力系统 section 高度为 720px，初始可见 `01:left / 02:top / 03:right`，隐藏卡位于 hidden 槽且 `visibility:hidden`、`pointer-events:none`；`ArrowRight`、`ArrowLeft`、滚轮上下和水平滚动均可单步切换，循环覆盖 `04/05/06`、`05/06/01`、`06/01/02`；移动 390x844 下 6 张卡为 `static` 纵向列表，`aria-hidden` 移除且水平溢出为 0 |
| 2026-07-10 | CS-016 详情页外侧留白纵深网格背景 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索源码确认 `case-depth-grid` 和 `.case-content-shell::after` 不再存在，新增 `case-outer-grid--left` / `case-outer-grid--right` 与 `--case-outer-grid-start: 840px`；preview 于 `http://127.0.0.1:4342/projects/ecommerce-review-copilot/?v=outer-grid-clear` 用可见 Chrome 检查用户红框对应区域 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；源码确认网格从内容壳内部撤出，改为 article 顶层外侧装饰；Chrome 可见检查确认网格位于页面最外侧左右留白，顶部侧边区域透明无网格线残影，正文和右侧摘要之间保持干净 |
| 2026-07-10 | CS-015 详情页内容区两侧纵深网格背景 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4341/` 启动；授权 HTTP 检查 `/projects/ecommerce-review-copilot/`；检索源码、构建产物和本地 HTML 中的 `case-depth-grid--left`、`case-depth-grid--right`、`background-size:32px 32px`、`background-size:68px 68px`、`display:none`；用可见 Chrome 打开详情页做人工视觉检查 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；preview 详情页返回 200 OK；源码与构建产物确认左右 `aria-hidden` 装饰层、近 / 远不同间距网格、浅米灰 / 浅青灰底雾和移动端隐藏规则已生效；可见 Chrome 检查确认左右留白出现浅色纵深网格且正文卡片仍在上层；Playwright 自带 Chromium 未下载，系统 Chrome headless 在当前环境异常退出，未完成自动截图断言 |
| 2026-07-10 | UX-020 详情页 Hero 主体下移避让装饰字 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 dev server 于 `http://127.0.0.1:4339/` 启动；浏览器检查 `/projects/ecommerce-review-copilot/?v=ux020-offset` 的 1280px 和 390px 视口，测量 `HAOLIN WU`、返回按钮、右侧判断链路面板、白色内容壳和水平溢出状态 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；1280px 视口下 `HAOLIN WU` 底部约 `72px`、返回按钮 top 约 `133px`，留出约 `61px` 空位；右侧面板 bottom 约 `583px`、白色内容壳 top 约 `646px`，保留约 `63px` 间距；390px 视口无水平溢出，返回按钮 top 约 `132px`，主体不再贴住装饰字 |
| 2026-07-10 | UX-019 暗色页眉透明化与英文名层级微调 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索构建产物中的 `.site-header--dark`、`background:transparent`、`position:absolute`、`--site-header-line-color:rgba(176,205,255,.24)` 和 `--editorial-letter-color:#1a2034`；授权 dev server 于 `http://127.0.0.1:4339/` 启动；浏览器检查首页和 `/projects/ecommerce-review-copilot/` 的 1280px / 390px 视口 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面；构建产物确认暗色页眉为透明覆盖层，分段线颜色增强，`HAOLIN WU` 字母色提亮；浏览器检查首页和详情页桌面 / 移动视口均无水平溢出，品牌与导航文字背景为 transparent，分段线不穿过字母实体 |
| 2026-07-10 | CS-014 详情页背景渐变 OKLab 细腻化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4338/` 启动，并检查 `/projects/ecommerce-review-copilot/`；检索源码与构建产物中的 `linear-gradient(180deg in oklab`、`.case-detail-page::before` 的 `top:548px` / `height:840px`、`tableValues='0 .018'`、中央 `50% 96%` 泛白 radial 和 `.case-detail-hero::after` 参数 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；preview 详情页返回 200 OK；源码与构建产物确认页面级雾面带向下拉长、停靠点加密、支持浏览器启用 OKLab 插值，dither 强度降低；中心 `50% 96%` 泛白光和 `.case-detail-hero::after` 的 `bottom:-170px`、`blur(58px)`、`opacity:0.72` 保持原值；本轮未启动浏览器截图级视觉 QA |
| 2026-07-10 | UX-018 暗色页眉字母内部残留横线彻底隐藏 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索构建产物中的 `.site-header--dark:after{content:none}`、`.site-header__editorial-name:before`、`.site-header__editorial-name:after`、`.site-header__editorial-gap:before`、`.site-header__editorial-space:before` 和 `--editorial-letter-color`；授权 preview 于 `http://127.0.0.1:4321/`；Chrome 打开 `/?v=ux018-solid` 和 `/projects/ecommerce-review-copilot/?v=ux018-solid` 做视觉检查 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面；构建产物确认整条暗色 header 下线已关闭，左右线段和字母间 / 词间线段由装饰字相关节点绘制，字母改用低对比实色；Chrome 视觉检查确认首页和详情页 `HAOLIN WU` 字母内部不再有明显横线，线仍保留在左右侧、字母间和词间 |
| 2026-07-10 | UX-017 暗色页眉英文名分段线可见修正 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索构建产物中的 `data-name="HAOLIN WU"`、2 个 `.site-header__editorial-word`、6 个 `.site-header__editorial-gap` 和 1 个 `.site-header__editorial-space`；授权 preview 于 `http://127.0.0.1:4337/`；Chrome 打开 `/?v=ux017-home` 和 `/projects/ecommerce-review-copilot/` 做视觉检查 | 通过；三项命令均通过，`astro check` 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面；构建产物确认首页和详情页均有 2 个词组、6 个字母间线段和真实词间空格；Chrome 视觉检查确认 `HAOLIN WU` 词间距可见，横线锚定 header 底部并只在字母间、词间和左右两侧露出 |
| 2026-07-09 | CS-013 详情页内容壳边缘融合柔化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 HTTP 检查 `http://127.0.0.1:4336/projects/ecommerce-review-copilot/`；浏览器检查 `.case-content-shell::before`、`.case-content-shell`、`.case-detail-page::before` 和 `.case-detail-hero::after` 的 computed style | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；浏览器检查确认 `.case-content-shell::before` 为 `blur(34px)`、`opacity:0.9`、`z-index:-1`，内容壳为 `isolation:isolate` 且 `overflow:visible`；`.case-detail-page::before` 仍为 `top:574px` / `height:660px` / `background-blend-mode:soft-light`；中心 `50% 96%` 光感仍存在；`.case-detail-hero::after` 保持 `bottom:-170px`、`filter:blur(58px)`、`opacity:0.72` |
| 2026-07-09 | CS-012 详情页内容壳两侧渐变抗色带细化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4336/` 启动；授权 HTTP 检查 `http://127.0.0.1:4336/projects/ecommerce-review-copilot/`；检索源码与构建产物中的 `--case-mist-dither`、`feTurbulence`、`.case-detail-page::before`、`top:574px`、`height:660px`、`background-blend-mode:soft-light`、中间 `50% 96%` 光感和 `.case-detail-hero::after` 参数 | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；源码与构建产物可检索到更长、更低对比的页面级衔接雾面带与极轻 SVG fractal-noise dither；中心 `50% 96%` 光感仍存在；`.case-detail-hero::after` 保持 `background: rgba(246, 247, 251, 0.78)`、`bottom:-170px`、`height:340px`、`filter:blur(58px)`、`opacity:0.72`；实际细腻度需用户在目标显示器上主观确认 |
| 2026-07-09 | CS-011 详情页内容壳两侧衔接雾面细腻化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 HTTP 检查 `http://127.0.0.1:4334/projects/ecommerce-review-copilot/`；检索源码与构建产物中的 `.case-detail-page::before`、`top:604px`、`height:500px`、左右 `ellipse 48% 74%` / `ellipse 58% 82%`、10 段纵向渐变、中间 `50% 96%` 光感和 `.case-detail-hero::after` 参数 | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；源码与构建产物可检索到更细密的页面级衔接雾面带；中心 `50% 96%` 光感仍存在；`.case-detail-hero::after` 保持 `background: rgba(246, 247, 251, 0.78)`、`bottom:-170px`、`height:340px`、`filter:blur(58px)`、`opacity:0.72`；本轮未完成截图级浏览器视觉 QA，衔接细腻度需用户在实际显示器上主观确认 |
| 2026-07-09 | UX-016 暗色页眉英文名横线避让字母 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 preview 于 `http://127.0.0.1:4335/`；浏览器检查首页和 `/projects/ecommerce-review-copilot/` 的 `HAOLIN WU` 位置、宽度、字母间距、header `::after` 下线位置、水平溢出和 console error logs；检索构建产物中的 `.site-header__editorial-space` 真实空格 | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；首页与详情页 `HAOLIN WU` 左上位置均为 `40px`、宽度约 `773px`、最大字母间距约 `6.48px`，没有向右拉散；导航下线 `::after` 位于 header `bottom:0`；构建产物确认 `.site-header__editorial-space` 内保留真实空格；浏览器 error logs 为空且无水平溢出 |
| 2026-07-09 | CS-010 详情页内容壳两侧背景衔接柔化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权 HTTP 检查 `http://127.0.0.1:4334/projects/ecommerce-review-copilot/`；检索源码与构建产物中的 `.case-detail-page::before`、`top:620px`、两侧 `ellipse 42% 68%` 衔接层、中间 `50% 96%` 光感和 `.case-detail-hero::after` 参数 | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；源码与构建产物可检索到页面级衔接雾面带；中心 `50% 96%` 光感仍存在；`.case-detail-hero::after` 保持 `background: rgba(246, 247, 251, 0.78)`、`bottom:-170px`、`height:340px`、`filter:blur(58px)`、`opacity:0.72`；本轮未完成截图级浏览器视觉 QA，衔接细腻度需用户在实际显示器上主观确认 |
| 2026-07-09 | UX-015 详情页判断链路面板 Apple-first 字体统一 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索源码与构建产物中的 `.case-panel-kicker`、`.case-chain-list span`、`font-family: var(--font-sans)` 和 `AIPM Judgment Chain` | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；源码和构建产物确认 `.case-panel-kicker` 与 `.case-chain-list span` 使用 `var(--font-sans)`；右侧判断链路面板仍保留 `AIPM Judgment Chain` 与 01-05 步骤列表 |
| 2026-07-09 | CS-009 详情页 Hero 背景渐变停靠点加密 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权启动 Astro preview 于 `http://127.0.0.1:4334/`；用授权 HTTP 检查 `/projects/ecommerce-review-copilot/`；检索源码与构建产物中的左右 `ellipse 82% 58%` / 12 段停靠点渐变、中间 `50% 96%` 光感和 `.case-detail-hero::after` 参数 | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；源码与构建产物可检索到左右 12 段停靠点渐变；中心 `50% 96%` 光感仍存在；`.case-detail-hero::after` 保持 `background: rgba(246, 247, 251, 0.78)`、`bottom:-170px`、`height:340px`、`filter:blur(58px)`、`opacity:0.72`；本轮未完成截图级浏览器视觉 QA，渐变细腻度需用户在实际显示器上主观确认 |
| 2026-07-09 | CS-008 详情页 Hero 背景左右过渡细腻化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权启动 Astro preview 于 `http://127.0.0.1:4333/`；浏览器打开 `/projects/ecommerce-review-copilot/` 后检查 Hero background computed style、`::after` 参数、水平溢出和过渡区域截图；检索源码中左右侧渐变层与中间光感原参数 | 通过；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；浏览器 1280x720 下详情页可访问，新增左右侧背景层生效，中心 `50% 96%` 光感仍存在；`.case-detail-hero::after` 保持 `background: rgba(246, 247, 251, 0.78)`、`bottom:-170px`、`height:340px`、`filter:blur(58px)`、`opacity:0.72`；无水平溢出；截图确认白色内容壳位置未改变 |
| 2026-07-09 | CS-007 详情页 Hero 标题断行与强调 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权启动 Astro preview 于 `http://127.0.0.1:4331/`；授权环境下使用 `curl -I` 检查 `/projects/ecommerce-review-copilot/`；检索构建产物中的标题拆分 DOM、渐变 class 和渐变色；尝试 Playwright 浏览器级测量 | 通过，带已知环境限制；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；构建产物可检索到 `case-hero-title--split`、`case-hero-title__line--accent`、`电商评论洞察与运营`、`决策 AI Copilot`、`#bfeeff` 和 `#c8b7ff`；Playwright package 可用但 Chromium executable 缺失，改用系统 Chrome channel 时浏览器异常退出，未完成自动 bounding box 测量 |
| 2026-07-09 | CS-006 修复详情页案例摘要 sticky 失效 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；通过内置浏览器临时桌面视口 1440x1000 测量 `.case-side-card`、`.case-side-rail`、祖先 overflow 和多个滚动位置下的卡片 top | 通过，带已知环境限制；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；桌面视口下 `.case-side-card` 为 `position: sticky`，`.case-side-rail` 提供约 9054px 右栏轨道；`.case-detail-page` 从 `overflow-x:hidden` / `overflow-y:auto` 修正为 `overflow-x:clip` / `overflow-y:visible`；滚动样本 `scrollY=500/900/1300/1769/2600/4200` 时 `cardTop` 均稳定为 `231px`，等于中线计算值；当前内置浏览器默认 560px 窄视口仍按移动端规则 `position:static` |
| 2026-07-09 | CS-005 修正详情页案例摘要栏内居中浮动 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权启动 Astro preview 于 `http://127.0.0.1:4330/`；使用 curl 检查 `/projects/ecommerce-review-copilot/` HTTP 状态；检索源码和构建产物中的 `case-summary-sticky-top`、`50svh` fallback、`case-side-card` 样式和错误 sticky 方案 | 通过，带已知环境限制；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；详情页 preview 返回 200 OK；源码和构建产物可检索到 `case-summary-sticky-top` 与 `50svh` fallback；详情摘要卡样式为 `position:sticky`、动态 `top`、`align-self:start` 和移动端 `position:static`；未检索到错误的 `top:50vh` / `translateY(-50%)` 方案；首页样本区核心文案仍保留 |
| 2026-07-09 | CS-004 优化样本 01 详情页拆解过程表达 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权启动 Astro preview，前次检查使用 `http://127.0.0.1:4328/`，本轮复核因 `4328` 已占用自动切到 `http://127.0.0.1:4329/`；使用 curl 检查首页和 `/projects/ecommerce-review-copilot/` HTTP 状态；检索源码和构建产物中的首页样本文案、旧样本副标题、详情页新结构类名和右侧摘要栏内 sticky 样式；尝试 Playwright 自动视觉 QA | 通过，带已知环境限制；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；首页和详情页 preview 均返回 200 OK，本轮在 `4329` 复核详情页返回 200 OK；源码与构建产物保留“一个样本，展示完整 AIPM 判断链路”“样本 01：从非结构化评论到运营决策”“查看拆解过程”，且未检索到旧的“样本 01：从非结构化评论到运营决策 Copilot”；详情页构建产物可检索到 `case-detail-hero`、`case-content-shell`、`AIPM Judgment Chain` 和右侧摘要卡 `top:96px` 栏内 sticky 样式，未检索到错误的 `top:50vh` / `translateY(-50%)`；Playwright 自动截图 QA 因本机缺少 browser executable 未执行 |
| 2026-07-09 | UX-014 首页关键 section 标题与副标题字号统一 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；授权启动 Astro preview 于 `http://127.0.0.1:4327/`；浏览器检查 1280px 与 390px 视口下 AIPM 能力系统、AI Product Operating Process、方法样本三处 h2 与副标题 computed typography、单行状态和水平溢出状态 | 通过，带已知环境限制；本地沙箱直接监听端口因 `listen EPERM` 失败，授权后 preview 启动成功；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；1280px 下三处标题均为 44px / 50.16px / 740，副标题均为 16px / 29.76px，能力系统标题单行显示且无水平溢出；390px 下三处标题均为 31.2px / 35.568px / 740，副标题一致且无水平溢出；浏览器 error logs 为空 |
| 2026-07-09 | UX-013 删除 Hero 重复徽标与能力标签 | 直接使用绝对路径 `pnpm run check`；随后使用 Codex bundled Node 写入 PATH 后执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；负向检索源码和构建产物中的 `hero-kicker`、`hero-keywords`、`AI PRODUCT BUILDER` | 通过，带已知环境限制；直接绝对路径 `pnpm run check` 因脚本内 `node: not found` 失败，符合当前本地环境限制；bundled Node 环境下三项命令通过，`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；源码与构建产物不再包含 `hero-kicker`、`hero-keywords` 和 `AI PRODUCT BUILDER` |
| 2026-07-09 | UX-012 Hero 英文名跨导航溢出微调 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；浏览器检查桌面 1280px 与移动 390px 视口下 `HAOLIN WU`、header、品牌文案和水平溢出状态 | 通过；bundled Node 环境下 `astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；桌面与移动端 `HAOLIN WU` 都从 header 内开始并跨过 header 底线进入 Hero，`AI Native Product Builder` 仍可见，旧的 `.hero-editorial-name` 不再存在，无水平溢出，浏览器 error logs 为空 |
| 2026-07-09 | UX-011 首页 Typography System 与方法样本视觉精修 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索源码和 `dist/index.html` 中的 `HAOLIN WU`、三行 Hero 核心句、`AI 产品原型构建者`、`Problem Framing` 和“找到 AI 真正应该解决的问题”；本地预览启动于 `http://127.0.0.1:4325/` 并检查 HTTP 状态；浏览器检查桌面 1280px 与移动 390px 视口 | 通过，带已知环境限制；bundled Node 环境下 `astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；构建产物可检索到本轮确认文案且未检索到旧的“样本 01：从非结构化评论到运营决策 Copilot”；本地预览首页返回 200 OK；桌面 1280px 和移动 390px 均无水平溢出，浏览器 error logs 为空 |
| 2026-07-09 | UX-010 Apple-first 字体栈与正文冷灰 token 精修 | 普通 PATH 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索 `src/styles/tokens.css` 中的 `-apple-system`、`SF Pro Text`、`Segoe UI`、`Roboto`、`PingFang SC`、`#1d1d1f`、`#6e6e73` 和 `#86868b` | 通过，带已知环境限制；普通 PATH 因 `node: not found` 失败；bundled Node 环境下 `astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；字体栈与正文灰色 token 已按本轮范围生效，非 Apple 设备有系统西文字体 fallback |
| 2026-07-08 | UX-009 首页能力驱动信息架构升级 | 普通 PATH 执行 `pnpm run check`；使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索 `dist/index.html` 中的 `AI PRODUCT BUILDER`、`AI Product Manager`、`AIPM Capability System`、`How I Build AI Products`、`方法样本 / 能力样本`、`样本 01`；负向检索 `精选案例`、`AI Product Prototype System`、`查看作品集`、`了解能力模型`、`暂无案例`、`coming soon`、`mock` 和 `没有真实用户`；授权启动 Astro preview 并通过浏览器检查桌面和 390px 移动视口 | 通过，带已知环境限制；普通 PATH 因 `node: not found` 失败；直接 `astro preview` 曾因 telemetry 写用户级偏好目录和沙箱端口权限失败，改用 `pnpm run preview` 并授权后启动于 `http://localhost:4321/`；bundled Node 环境下 `astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；关键首页 IA 文案可检索，旧文案未检索到；桌面视口无水平溢出，Hero 底部可见下一段能力系统提示；390px 移动视口无水平溢出，Hero 能力节点、AIPM 能力节点和 5 阶段流程数量正确，AIPM 节点纵向堆叠无重叠；浏览器 error logs 为空 |
| 2026-07-08 | UX-008 首页精选案例重构为能力样本 | 普通 PATH 执行 `pnpm run check`；使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索 `dist/index.html` 中的“能力样本”、“方法样本：我如何拆解一个 AI 产品机会”、“样本 01：从非结构化评论到运营决策 Copilot”和“查看拆解过程”；负向检索“精选案例”、“暂无案例”、“coming soon”、“mock”和“没有真实用户”；授权启动 `astro preview` 并检查首页 HTTP 状态 | 通过，带已知环境限制；普通 PATH 因 `node: not found` 失败，符合当前本地环境限制；bundled Node 环境下 `astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；关键方法样本文案可检索；负向文案未检索到；`http://127.0.0.1:4324/` 返回 200 OK |
| 2026-07-08 | UX-007 首页 Hero 个人品牌识别与能力系统精修 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索源码和 `dist/index.html` 中的个人品牌签名、副标签、能力节点编号、系统日志标签和旧重复短语 | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；构建产物可检索到“巫浩林”、`AI 产品原型构建者 / Personal Portfolio`、`LOG 01`、`system-node__code` 和 `AI Product Prototype System`；源码与构建产物不再检索到 `巫浩林 · AI Native Product Builder Portfolio` |
| 2026-07-08 | UX-006 首页暗色玻璃质感 Hero 改版 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索 `dist/index.html` 和 `dist/_astro/*.css` 中的新 Hero 标题、CTA、暗色页眉、能力系统面板、六个能力节点与六个编号标签；尝试本地浏览器预览和截图级 QA | 通过，带已知限制；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；构建产物可检索到 `把业务问题拆成`、`查看作品集`、`了解能力模型`、`AI Product Prototype System`、`业务拆解` 和 `06 Portfolio Delivery`；本地端口访问因沙箱端口权限受限，Browser `data:` 预览因安全策略受限，未完成截图级视觉 QA |
| 2026-07-08 | UX-005 恢复页眉产品身份文本 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索 `dist/index.html` 中页眉身份和 Hero 姓名；检查本地预览首页 HTTP 状态 | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；首页构建产物页眉可检索到 `AI Native Product Builder`，Hero 可检索到“巫浩林”，未恢复 `identity-mark` / `identity-role`；`http://127.0.0.1:4323/` 返回 200 OK |
| 2026-07-08 | UX-004 首页姓名标识回收为文字署名 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint`；检索源码与 `dist/index.html` 中的姓名和旧 identity 关键词；授权启动 `astro preview` 并检查首页 HTTP 状态 | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；首页构建产物可检索到“巫浩林”，源码不再包含 `identity-mark` / `identity-role` 或可见的 `AI Native Product Builder`；`http://127.0.0.1:4323/` 返回 200 OK |
| 2026-07-07 | UX-003 首页姓名身份标识视觉优化 | 使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint` | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/` |
| 2026-07-07 | UX-002 首页封面能力证明链条 | `pnpm run check` 普通 PATH 先因 `node: not found` 失败；随后使用 Codex bundled Node / pnpm 执行 `pnpm run check`、`pnpm run build`、`pnpm run lint` | 通过；bundled Node 环境下 `astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；普通 PATH 缺少 `node` 是已知本地环境限制，不代表项目验证失败 |
| 2026-07-07 | CS-002 扩展 Case Study Detail 正式案例展示骨架 | `pnpm run check`；`pnpm run build`；`pnpm run lint`；构建产物关键区块检索；构建产物 `TBD` 检索 | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；构建产物可检索到 `Product Solution`、`Evaluation Metrics`、`Outcome / Demo Evidence`、`Reflection`、`人的控制点` 和 `实现说明与取舍`；构建产物未检索到直接暴露的 `TBD` |
| 2026-07-07 | CS-001 创建 Ecommerce Review Copilot 正式 Case Study 内容骨架 | `pnpm run check`；`pnpm run build`；`pnpm run lint` | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/ecommerce-review-copilot/`；`foundation-placeholder` 已归档且不再生成公开静态路由 |
| 2026-07-07 | UX-001 前端文案中文优先表达 | `pnpm run check`；`pnpm run build`；`pnpm run lint`；`curl -s http://127.0.0.1:4322/` 检查中文导航、CTA、案例卡片与流程标签 | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/foundation-placeholder/`；本地 dev server 页面可读到中文优先文案，必要英文仅保留在品牌名和 AI Workflow / Agent / Human-in-the-loop / AI Boundary 等术语中 |
| 2026-07-07 | FN-001 最小 Astro + TypeScript 基座 | `pnpm add astro @astrojs/mdx @tailwindcss/vite tailwindcss`；`pnpm add -D @astrojs/check typescript`；`pnpm rebuild esbuild`；`pnpm run check`；`pnpm run build`；`pnpm run lint`；`pnpm exec astro dev --host 127.0.0.1 --port 4321 --force`；`curl -I http://127.0.0.1:4322/`；`curl -I http://127.0.0.1:4322/projects/foundation-placeholder/` | 通过；`astro check` 结果 0 errors / 0 warnings / 0 hints；`astro build` 生成 3 个静态页面：`/`、`/projects/`、`/projects/foundation-placeholder/`；dev server 因 4321 被占用自动使用 `http://127.0.0.1:4322/`，首页和动态详情页均返回 200 OK；依赖安装曾因沙箱网络和 Astro telemetry 写入用户级目录受阻，已通过授权联网安装和 `ASTRO_TELEMETRY_DISABLED=1` 解决 |
| 2026-07-07 | CP-003 Foundation 前状态同步 | 读取 PRD / architecture / backlog / iteration-log / decision-log / testing / tech-stack-decision；`find apps -maxdepth 4 -type f -print`；`diff -u AGENTS.md CLAUDE.md`；关键字检索当前阶段、技术路线、视觉系统和下一步路由；`git status --short` | 通过，带已知限制；`apps/` 仍只有 `.gitkeep`，AGENTS/CLAUDE 仅镜像声明互指差异，下一步仍为 `prddev-foundation`；当前目录不是 Git 仓库，`git status --short` 不可用 |
| 2026-07-07 | CP-002 同步 PRD 技术路线与视觉系统状态 | 读取 `docs/prd.md`、`docs/tech-stack-decision.md`、`docs/visual-system.md`；`rg` 检查“技术路线未决”“待技术路线确认”、Astro-powered route、`docs/visual-system.md`、Astro Content Collections + MDX + Zod schema 和视觉与体验边界；`find apps -maxdepth 4 -type f -print` | 通过；`docs/prd.md` 已同步当前技术路线和视觉系统状态，未进入应用初始化，`apps/` 仍只有 `.gitkeep` |
| 2026-07-07 | VS-001 新增 Portfolio 视觉系统护栏 | `rg --files docs`；关键术语、路径、`VS-001`、`D-004` 检索；冲突关键词检查 `apps/portfolio`、负 `letter-spacing`、过大圆角 token；`diff -u AGENTS.md CLAUDE.md`；`find apps -maxdepth 4 -type f -print` | 通过；`docs/visual-system.md` 已存在，AGENTS/CLAUDE 仅镜像声明互指差异，`apps/` 仍只有 `.gitkeep`，未初始化应用；`docs/prd.md` 的文档状态仍保留旧的“技术路线未决”表述，建议后续 checkpoint 同步 |
| 2026-07-07 | TR-001 确认 Astro-powered Editorial Product Experience System | 读取并更新 `docs/tech-stack-decision.md`、`docs/tech-radar.md`、`docs/decision-log.md`、`docs/architecture.md`、`docs/content-model.md`、`docs/roadmap.md`、`docs/backlog.md`、`docs/testing.md`、`docs/iteration-log.md`；`rg` 检查 technical route 状态、visual-system 文件引用、foundation 下一步；`find apps -maxdepth 4 -type f -print` | 通过；primary route 已写入，`docs/visual-system.md` 未创建，`apps/` 仍只有 `.gitkeep`，下一步已更新为 `prddev-foundation`；`docs/prd.md` 的文档状态仍保留旧的“技术路线未决”表述，按本轮 tech-route 边界未改，建议后续 checkpoint 同步 |
| 2026-07-07 | CP-001 Portfolio 文档一致性复核 | 读取 PRD / architecture / backlog / iteration-log / decision-log / testing；扫描 AGENTS / CLAUDE / tech-stack-decision / roadmap；`find apps -maxdepth 4 -type f -print`；关键字检索下一步路由与边界 | 通过；文档状态一致，`docs/tech-stack-decision.md` 仍为未决，foundation / coding 被阻止，`apps/` 仍只有 `.gitkeep`，下一步唯一建议已更新为 `prddev-tech-route` |
| 2026-07-07 | PB-001 纳入 Portfolio 内容系统骨架 | `rg --files docs`、关键字检索、`diff -u AGENTS.md CLAUDE.md`、`find apps -maxdepth 3 -type f` | 通过；新增内容系统文档存在，关键边界可检索，AGENTS/CLAUDE 除镜像声明互指外内容一致，`apps/` 仍只有 `.gitkeep` |
