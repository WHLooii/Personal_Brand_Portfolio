# Tests

Foundation currently uses Astro build, Astro check, and content schema validation as the executable verification layer.

Add Playwright or component-level tests only when a later increment introduces interaction or visual QA scope.
